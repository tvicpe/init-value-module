import { InitValuePipe } from './init-value.pipe';

describe('InitValuePipe', () => {
  it('create an instance', () => {
    const pipe = new InitValuePipe();
    expect(pipe).toBeTruthy();
  });
});
